# jZen — where the framework's rules live

**This folder holds no documents, only pointers.** Prudent depends on the jZen framework, and while
that dependency is a **sibling checkout at `../jZen`** rather than a published package, jZen's rules
are read from that checkout directly. Nothing is copied here: a copy drifts, and a drifted rule is
worse than an absent one.

`../jZen` is **read-only** from this repository.

| jZen document | Path | What it is |
|---|---|---|
| `MANIFESTO.md` | `../../../jZen/docs/architecture/MANIFESTO.md` | How jZen applies Zen Architecture to a concrete stack |
| `BLUEPRINT.md` | `../../../jZen/docs/architecture/BLUEPRINT.md` | The jZen architecture as built |
| `STANDARDS.md` | `../../../jZen/docs/architecture/STANDARDS.md` | The rules — the one to check a change against |
| `DECISIONS.md` | `../../../jZen/docs/architecture/DECISIONS.md` | jZen's ADR archive — **wins on conflict with the three above** |
| `ROADMAP.md` | `../../../jZen/docs/architecture/ROADMAP.md` | jZen's build sequence and open backlog |

Reference implementations are in the same checkout; the `jzen-reference` skill maps them.

## Findings Prudent has reported upstream

A framework need is **reported, never forked** — `../jZen` is read-only, and a copy of framework
source in this repository is the thing that drift is made of. Findings are tracked here so a later
session can tell "reported and waiting" from "fixed and consumed" from "nobody has raised this".

| Finding | Upstream | State |
|---|---|---|
| `protoc-gen-dart` writes imports as **filesystem-relative paths** and has no equivalent of Go's `M` mapping, so an application proto importing `zen/v1/common.proto` produced code pointing at a path that resolves to nothing inside the application's package. Satisfying it by regenerating `zen.v1` into the application's tree is worse and silently so: the copy is a *different Dart type*, so the framework's own API stops type-checking against it. (The Java side was always clean — `zen.v1` resolves from the `zen-proto` jar.) | [#54](https://github.com/jZenDev/jZen/issues/54) → [PR #55](https://github.com/jZenDev/jZen/pull/55) | **Fixed and consumed.** Prudent's side is ADR-006 (the contract and its tracking rule) and ADR-007 (the workaround's deletion condition). jZen moved its messages to the public `zen_transport/lib/generated`, and `zen:generate:proto:dart` now passes protoc both contract roots with `-I` but only the application's protos as arguments, rewrites the leftover relative imports to `package:zen_transport/generated/zen/v1/…`, and refuses if a `zen/v1` file lands in the application's tree. |
| The contract loop was hardcoded to jZen's own tree, so `Taskfile.app.yml` could not carry it and an application could not include it. Its `generate:proto:dart` also *skipped* with exit 0 on an empty proto directory — right for a framework skeleton, wrong for an application, where an empty contract root is a broken checkout. | [#54](https://github.com/jZenDev/jZen/issues/54) → [PR #55](https://github.com/jZenDev/jZen/pull/55) | **Fixed and consumed.** Prudent's side is ADR-007. Prudent deleted its own `generate:proto:dart` and delegates to `zen:generate:proto:dart`. The rest of the loop followed in [#74](https://github.com/jZenDev/jZen/issues/74) (Prudent ADR-027); the server build, the `run:*` stack (pending [#77](https://github.com/jZenDev/jZen/issues/77)) and the deploy stay Prudent's own. |
| Nothing catches a fully-qualified class reference used inline instead of an import (e.g. `new prudent.server.account.AccountBalance(...)` where `AccountBalance` is already, or could be, imported) — both compile identically. Prudent added PMD's `UnnecessaryFullyQualifiedName` rule to its own `server/pom.xml` (Phase 4) and found a real instance doing so (`UserScopingTest.java`). Also flagged that `maven-pmd-plugin`'s *default* resolves PMD 6.55.0, whose bundled ASM cannot parse a Java 25 class file (`Unsupported class file major version 69`) — every file fails to process and the plugin reports `BUILD SUCCESS` anyway, a vacuous gate; 3.27.0 (PMD 7.14.0) parses correctly. | [#61](https://github.com/jZenDev/jZen/issues/61) → [PR #62](https://github.com/jZenDev/jZen/pull/62) | **Fixed and consumed (Phase 5).** No Prudent ADR owns this one — the decision lives in `server/pom.xml` and `server/pmd-ruleset.xml`, and it is the only finding here without an entry in the log. jZen took the proposed shape exactly: `zen-parent`'s `pluginManagement` now supplies `maven-pmd-plugin`'s version (`${maven-pmd-plugin.version}` = 3.27.0) and nothing else, pluginManagement-only, so each consuming module still opts in rather than every inheriting module being linted unconditionally. Prudent's `server/pom.xml` **deleted its own `<version>3.27.0</version>` line** and re-ran `./mvnw test-compile`, confirming the version still resolves to 3.27.0/PMD 7.14.0 purely from the inherited `pluginManagement` and the rule still runs (`task test:server` stayed green, 79/79). `server/pmd-ruleset.xml` and the plugin's `<configuration>`/`<executions>` stay Prudent's own — jZen manages the version, not the ruleset content or the decision to run it, which is application-specific by the finding's own proposal. |
| `UserRetentionService.anonymiseExpiredAccounts()` (GDPR Art. 5(1)(e) dormancy retention) mutates a dormant `users` row directly, in its own transaction, and fires **no event** an application could observe — unlike `UserRegistered`, which exists for exactly the symmetric case on the way in (jZen ADR-007). An application whose own tables reference `user_id` has no framework-provided hook to cascade its own cleanup when an account is anonymised, so it either duplicates the framework's anonymisation-marker convention (`anon_<uuid>@deleted.invalid` — currently package-private in `UserRetentionService`, so an application matching it is coupled to a literal that could change silently) or leaves its own rows behind indefinitely. Prudent's workaround (Phase 5 §5, `prudent.server.retention.PrudentRetentionCleanupJob`) is a second `ZenJob` that scans `users` for the marker and sweeps Prudent's own tables — functionally correct, proven by `PrudentRetentionCleanupJobTest`, but a scan is a weaker mechanism than an event and the coupling to a private string literal is exactly the kind of thing that breaks silently. Proposed fix: `UserRetentionService` fires a `UserAnonymised(UUID userId)` CDI event per row, mirroring `UserRegistered`'s shape. | [#63](https://github.com/jZenDev/jZen/issues/63) → [PR #66](https://github.com/jZenDev/jZen/pull/66) | **Fixed and consumed (2026-08-18).** Prudent's side is ADR-019, superseded by ADR-022. jZen took the proposed shape exactly: `UserAnonymised(UUID)` fired per row, synchronously, from inside the anonymising transaction, so a cascade that throws rolls the anonymisation back with it. Prudent deleted `PrudentRetentionCleanupJob` and its marker literal outright and replaced them with `PrudentRetentionCleanup`, an observer joining that transaction with `@Transactional(MANDATORY)`. The test was rewritten at the same time and matters more than the class did: it now drives `UserRetentionService.anonymiseExpiredAccounts()` for real instead of fabricating a row from the marker literal, so it can fail when the framework changes — proven by breaking the cascade and watching it go red. `JZEN_REF` bumped to `9a0b5cc` in both workflows, which is what makes CI build against the fix rather than the commit before it. |
| `zen-jobs` is **silently inert** when `zen.jobs.trigger.token` is unconfigured. The only warning lives in `JobTriggerAuthenticator.isAuthorized()` and fires *when the trigger is called* — but an application that has not configured the token has not created the scheduler entry either, so nothing calls it and the warning never fires. The framework is loud about a half-configured deployment and silent about a wholly unconfigured one, which is backwards. `JobScheduler.seedRegisteredJobs(@Observes StartupEvent)` already knows how many `ZenJob` beans are registered, so at boot the framework holds both facts — *N jobs registered* and *no credential by which any can run* — and does not check them together. Found in Prudent, whose `application.properties` had no `zen.jobs` configuration at all: every suite green, every request served correctly, and ADR-019's GDPR retention sweep would never have run in production. Proposed fix: a startup `WARN` when jobs are registered and the token is blank, plus a health-check contribution so an application's own `verify:deploy` can assert it. | [#65](https://github.com/jZenDev/jZen/issues/65) → [PR #68](https://github.com/jZenDev/jZen/pull/68) | **Fixed and consumed (2026-08-19).** jZen took the proposed shape: `JobScheduler` now checks both facts at boot and warns when jobs are registered with no trigger token, covered by its own `JobSchedulerInertnessTest`. Prudent needed no code change — but it did need `%test.zen.jobs.trigger.token`, because the new warning fired on every test run, where it is true of nothing, and a warning that always fires is one nobody reads. **`verify:deploy`'s scheduler assertion stays**: the startup warning proves a token exists, and cannot see whether anything is actually calling the trigger — the two check different halves. See ADR-021 for the deploy contract this surfaced from and ADR-023 for the consumption. The missing configuration was Prudent's to supply (ADR-007's split) and is supplied — `%prod.zen.jobs.trigger.token` reads `ZEN_JOBS_TRIGGER_TOKEN`, empty by default so an unset secret keeps failing closed, and `deploy:cloudrun`'s runbook creates the scheduler entry. Prudent does **not** wait on the framework here: `verify:deploy` asserts the Cloud Scheduler entry exists, so the inert state fails a gate Prudent owns. A startup signal upstream would still catch it one layer earlier, on the first `%prod` boot rather than at the first deploy verification. |
| `task zen:generate:l10n` and `task zen:test:client`'s own internal call to it discover localized packages with `git ls-files`, **scoped to the repository the task runs in**. Called from an application's root (Prudent's, via the `zen:` include), it can only ever find *that* application's own `l10n.yaml` files — never a framework package's, since `client/zen_ui_identity/l10n.yaml` etc. belong to a **different git repository** a sibling-checkout `git ls-files` structurally cannot see. Invisible on a developer's machine, where jZen's own local test runs have already generated and cached `zen_ui_identity`'s and `zen_ui_navigation`'s localizations on disk; **a fresh CI clone of both repositories has no such history**, and Prudent's first real CI run failed on exactly this (`IdentityLocalizations isn't a type`) in three separate jobs, because Prudent's own screens reach those types through the framework's `LoginScreen`/`ProfileScreen`/`ZenNavigation` widgets. Worked around per-job (`.github/workflows/ci.yml`): an extra step runs `task zen:generate:l10n` a second time with `working-directory: jZen`, so `git ls-files` resolves against jZen's own tracked files instead. Proposed fix: `zen:generate:l10n` (and any other task discovering by `git ls-files`) should also walk a `../jZen`-relative root when one is configured, or the framework should expose a task an application can call to generate *just* the framework's own localizations without a multi-repo consumer having to `cd` into the sibling checkout by hand. | [#64](https://github.com/jZenDev/jZen/issues/64) → [PR #67](https://github.com/jZenDev/jZen/pull/67) | **Fixed and consumed (2026-08-19), in two rounds.** jZen added `zen:framework:prepare`, which runs `generate:l10n` and `pnpm install` inside its own root. Prudent's `gates` job now calls it and both hand-written workarounds are deleted. The two Flutter runner jobs needed only half of it, which produced [#69](https://github.com/jZenDev/jZen/issues/69) and its own row below; since that landed, **no CI job names a path inside the framework checkout at all** — which was #64's whole point. |
| The admin panel's TypeScript scaffold-from-source seam has the same shape one layer down. `admin/src` imports `@jzen/admin-core` from `../jZen/admin/src` via a `paths` alias (ADR-016), so TypeScript resolves `react-admin`/`ra-data-simple-rest`/`react` — jZen's `admin/package.json`'s own devDependencies — by walking up from `../jZen/admin/src` looking for a `node_modules`. Nothing has ever run `pnpm install` **inside** `../jZen/admin` on a fresh checkout, so there is none to find. Invisible on a developer's machine that already has jZen's `admin/` pnpm-installed from unrelated local work on the framework; real on the fresh CI clone that caught it (`Cannot find module 'react-admin'`). Worked around with an extra `pnpm install` step scoped to `jZen/admin` before anything typechecks against it. Proposed fix: the same shape as the finding above — a multi-repo consumer has no task-level way to ask the framework to resolve its own scaffold's dependencies without reaching into the sibling checkout by hand. | [#64](https://github.com/jZenDev/jZen/issues/64) → [PR #67](https://github.com/jZenDev/jZen/pull/67) | **Fixed and consumed (2026-08-19).** `zen:framework:prepare` installs the scaffold's dependencies in jZen's own `admin/`, so Prudent's hand-written `pnpm install` step is deleted outright. This half needed no caveat: the only job that typechecks the admin panel is `gates`, which has Node anyway. |

| `zen:framework:prepare` (#64's own fix) is all-or-nothing: it generates jZen's l10n **and** `pnpm install`s its admin scaffold, so a consumer needing only the first cannot ask for it and fails on the second where there is no Node toolchain. Prudent's `gates` job wanted both; its `android-runner` and `windows-runner` jobs build Flutter runners, have no Node, and have no reason to acquire it — so they kept reaching into the sibling checkout by hand, which is exactly the coupling #64 set out to remove, surviving in the one place the framework's answer did not fit. Proposed: split into `prepare:l10n` / `prepare:admin`, keeping `prepare` as both; the weaker alternative (skip the admin half audibly when `pnpm` is absent) cannot tell "this job does not need it" from "this job needs it and the toolchain is missing". | [#69](https://github.com/jZenDev/jZen/issues/69) → jZen `3a450b2` | **Fixed and consumed (2026-08-19).** Prudent's side is ADR-023, extended by ADR-024. jZen took the split. Both runner jobs now call `zen:framework:prepare:l10n` from Prudent's own root, and the last two `working-directory: jZen` steps are gone — every touch of the framework checkout goes through a `zen:` task, and the only place CI still names the directory is the `actions/checkout` that creates it. |
| The rest of the contract loop, the dependency install and the local stack existed only in jZen's own zen_demo-shaped `Taskfile.yml`, so Prudent hand-rolled `generate:proto:java`, `generate:api*`, `generate:l10n`, `sync:contracts`/`sync:verify` and `deps:admin` — each a copy that resembles the framework's and drifts. Issue #20 on Prudent's side (one command to install / run / regenerate) is the same gap from the consumer's end. Proposed: move the whole loop + `deps` + a `run:dev` rollup into `Taskfile.app.yml` against vars, and split `sync:contracts` into `generate` (always green) + `verify:contracts` (the gate). | [#74](https://github.com/jZenDev/jZen/issues/74) → jZen `b443780` (jZen ADR-049) | **Fixed and consumed (2026-08-30).** Prudent's side is ADR-027. Prudent deleted all ten tasks and delegates through three thin aliases (`generate`, `verify:contracts`, `deps`); `server/openapi.json` is no longer tracked (the admin panel's `schema.generated.ts` is the watched artifact); `JZEN_REF` bumped to `b443780`; CI jobs that need one tier call `zen:deps:client` / `zen:deps:admin` by name. `task generate` produced byte-identical `*.pb.dart` and `schema.generated.ts`. |
| jZen's generic `run:client` / `run:dev` (from #74) drop the operator guards Prudent's own `run:*` carry: a CORS-allowlist preflight that turns an opaque browser `403` into a fix, a pre-launch backend health check, `DEVICE`→`ZEN_PLATFORM` inference with an explicit failure, and local-Supabase mailbox/Studio URL hints. All app-agnostic — nothing names Prudent — so they belong in the shared file, not reinvented per consumer. | [#77](https://github.com/jZenDev/jZen/issues/77) → [PR #78](https://github.com/jZenDev/jZen/pull/78) → jZen `0038919` | **Fixed and consumed (2026-08-30).** Prudent's side is ADR-028. All four guards are in `zen:run:client` / `zen:run:dev` / `zen:run:supabase`. Prudent's `run:supabase` and `run:dev` are now one-line aliases to those (the bare names kept, the way jZen aliases its own for zen_demo); `ZEN_WEB_PORT: 8090` and `APP_CLIENT_DIR: client` are set on the include. (`run:server` / `run:client` kept a Prudent body until #79/#80 landed — see the next two rows and ADR-029.) |
| `zen:run:dev`'s `desc` promises "one Ctrl-C tears it all down"; it does not. One SIGINT kills the backend and Chrome fast (ports free within a second), but `flutter run` traps SIGINT for its own interactive shutdown and does not exit, and the task has `trap … EXIT` only — no `INT`/`TERM` handler — so it stays blocked on the foreground `flutter run`, then sits ~15 s in the `lsof` backstop before exiting `status 1`, flutter's `Press [e]…` prompt on screen the whole time. It reads as hung, so a user hits Ctrl-C several more times. Proposed: `trap 'stop_all' INT TERM EXIT`; run `flutter run` and the server subshell as tracked children (captured PID/PGID, `setsid` where available) and have `stop_all` kill them directly, keeping the port sweep as backstop. | [#80](https://github.com/jZenDev/jZen/issues/80) → [PR #82](https://github.com/jZenDev/jZen/pull/82) → jZen `b74b73a` | **Fixed and consumed (2026-08-31, ADR-029).** jZen runs the client with stdin closed (no interactive key handler → one Ctrl-C shuts it down) and kills it from a `--pid-file` in the exit handler. Verified against `b74b73a`: one SIGINT to `task run:dev`'s process group tore the whole stack down, ports free, no hang. **Did not hold on a real terminal — see #87/#89 below.** |
| `run:dev`'s one-Ctrl-C teardown took three more rounds after #82. **(a)** #82's stdin-closed client still needed two SIGINTs on Flutter 3.47.1, and the backgrounded server shared the terminal's process group, so the first Ctrl-C killed the server directly, not `stop_all` ([#83](https://github.com/jZenDev/jZen/issues/83) → [#84](https://github.com/jZenDev/jZen/pull/84): moved the rollup into `scripts/run-dev.sh`). **(b)** Under #84's `set -m` the backgrounded server hit **SIGTTIN** on Quarkus dev's aesh TTY read and was stopped — `run:dev` failed every run with "server did not become healthy" ([#87](https://github.com/jZenDev/jZen/issues/87) → [#88](https://github.com/jZenDev/jZen/pull/88): `< /dev/null` on the server subshell). **(c)** #84 left `set -m` on for the whole script, so the `sleep` in the wait loop grabbed the terminal each second and a terminal Ctrl-C landed on `sleep`, not the script — the stack could not be stopped ([#89](https://github.com/jZenDev/jZen/pull/89): `set -m` on only around each `&`). | [#83](https://github.com/jZenDev/jZen/issues/83) / [#87](https://github.com/jZenDev/jZen/issues/87) → [#84](https://github.com/jZenDev/jZen/pull/84) / [#88](https://github.com/jZenDev/jZen/pull/88) / [#89](https://github.com/jZenDev/jZen/pull/89) → jZen `8a21f83` | **Fixed and consumed (2026-09-01, ADR-030).** `JZEN_REF` bumped to `8a21f83`; `Taskfile.yml`'s `run:dev` comment/summary corrected to name `scripts/run-dev.sh` (per-`&` process groups + trapped INT/TERM, both children stdin-closed). Prudent needed no code change. Verified end to end under a pseudo-terminal: server healthy, client on `:8090`, one Ctrl-C (`0x03` to the pty) → `stop_all` ran, `prudent-server stopped`, ports free. |
| Two gaps remain in jZen's single-tier run primitives after #78. `zen:run:server` does not `eval "$(supabase status -o env)"` the way `zen:run:dev` does, so a standalone `task run:server` leaves `SUPABASE_KEY` unset and every auth call 500s (a consumer's `%dev` config defaults the anon key to empty — it is per-stack, not a constant). And `zen:run:client` adds `--dart-define=ZEN_AUTH_REDIRECT_URI` on every platform when the var is set, but a custom-scheme redirect is native-only — passing it to a web build makes the server refuse the address as `invalid_redirect`. Proposed: `run:server` gains `deps: [run:supabase]` + the same credential eval; `run:client` / `run:dev` pass the redirect only when the resolved platform is not `web`. | [#79](https://github.com/jZenDev/jZen/issues/79) → [PR #81](https://github.com/jZenDev/jZen/pull/81) → jZen `b74b73a` | **Fixed and consumed (2026-08-31, ADR-029).** jZen took both fixes exactly as proposed. Prudent deleted its own `run:server` and `run:client` and delegates to `zen:run:*`; the whole `run:*` family is now bare-name aliases. `ZEN_AUTH_REDIRECT_URI: prudent://auth-callback` set on the include (applied native-only by jZen). Verified: `task run:server` brought up Supabase + wired the anon key + reached "Profile dev activated". |

**Checking whether a fix has landed is nobody's job unless someone does it.** Nothing here polls, and the issues deliberately do not ask jZen to edit Prudent's documents — so a fix can land upstream and this table will keep claiming the workaround is needed. Check on every jZen bump, which is the moment it could become true:

```
gh issue list --repo jZenDev/jZen --state all --search "63 64 65 74 77 79 80 81 82"
```

**Consuming a fix is a deliberate step, not automatic.** A merged PR upstream does not change this
repository until someone deletes the local workaround and proves the framework's version green
here; and a filed issue is never a reason to pre-emptively reshape Prudent around a change that
does not exist yet. Because jZen is consumed by **path**, "which fix am I on" is a commit — run
`task zen:info`.

## What is Prudent's, and what is jZen's

- **[`docs/zen-architecture.md`](../zen-architecture.md) is Prudent's own cornerstone** — the design
  philosophy this product is built on. It is permanent and does not depend on jZen.
- **Prudent's rules are Prudent's**, in `docs/` and `docs/DECISIONS.md` (its own ADR numbering from
  ADR-001). jZen's documents constrain Prudent only where Prudent actually consumes the framework —
  the transport seam, the contract, auth, migrations. They are not Prudent's house style.
- Where a jZen rule and a Prudent rule conflict on something Prudent owns, **Prudent's wins**, and
  the divergence is recorded as a Prudent ADR.

## This folder is temporary

It exists because jZen is consumed from a filesystem path. **When jZen publishes its packages and
Prudent depends on them by version, this folder is deleted** — a versioned dependency's
documentation travels with the package, and there is no sibling checkout left to point at. That
removal is a step in the publishing ADR, not a cleanup. `docs/zen-architecture.md` is unaffected.
